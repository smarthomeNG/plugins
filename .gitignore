*.pyc
*.jar
*.tgz
*.zip

# Logs
*.log

# Unittest and coverage reports
.cache
.coverage
.tox
nosetests.xml
*.egg-info

# OSX specific files
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# don't upload private plugins or symlinked dirs
/priv_*

# don't upload plugins loaded from develop to a release installation
/*_dev/

# Pycharm settings
/.idea
/.vs

# VSCode settings
.vscode/settings.json

# pyright stub packages (tools/pyright_stub_audit.py) — local dev aid,
# CI lints with ruff, not pyright; regenerate with --apply-create if needed
/typings/

# Node sidecar dependency for the matter plugin (dev/matter/matter-integration-plan.md
# in the core repo) - package.json and package-lock.json are tracked for
# reproducible installs, the installed package itself is not.
matter/sidecar/node_modules/
