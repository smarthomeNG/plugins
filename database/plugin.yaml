# Metadata for the Smart-Plugin
plugin:
    # Global plugin attributes
    type: system                  # plugin type (gateway, interface, protocol, system, web)
    description:                  # Alternative: description in multiple languages
        de: 'Database plugin, mit Unterstützung für SQLite 3 und MySQL'
        en: 'Database plugin, with support for SQLite 3 and MySQL'
    maintainer: ohinckel, msinn, onkelandy
    tester: psilo909, brandst, aschwith
    state: ready
    keywords: database
    support: https://knx-user-forum.de/forum/supportforen/smarthome-py/1021844-neues-database-plugin

    version: 1.7.0                # Plugin version
    sh_minversion: '1.12.2'         # minimum shNG version to use this plugin
#    sh_maxversion:                # maximum shNG version to use this plugin (leave empty if latest)
    multi_instance: True           # plugin supports multi instance
    restartable: unknown
    startorder: early              # set start priority of plugin (early/normal/late)
    classname: Database            # class containing the plugin

parameters:
    # Definition of parameters to be configured in etc/plugin.yaml
    driver:
        type: str
        default: 'sqlite3'
        description:
            de: 'Spezifiziert das DB-API2 Treiber Modul'
            en: 'specifies the DB-API2 driver module (e.g. Python includes the SQLite driver by importing the module sqlite3, to use it here just set the driver parameter to the module name sqlite3)'
    connect:
        type: list(str)
        description:
            de: 'Spezifiziert die Verbindungsparameter, welche genutzt werden, um die connect() Funktion des DB API 2 aufzurufen.'
            en: 'specifies the connection parameters which is directly used to invoke the connect() function of the DB API 2 implementation (for SQLite lookup here, other databases depends on implementation). An example connect string for pymysql could be connect = host:127.0.0.1 | user:db_user | passwd:db_password | db:smarthome'
    sqlite_wal_mode:
        type: bool
        default: False
        description:
            de: "Nur für SQLite3: Auf True setzen, um die Datenbankdatei in den WAL-Journal-Modus zu versetzen - erlaubt einem Schreibvorgang, parallel zu Lesevorgängen weiterzulaufen, statt sie zu blockieren (relevant, da dieses Plugin zwei unabhängige Verbindungen zur selben Datei offen hält). ACHTUNG: Dies ist praktisch eine Einwegentscheidung - WAL ist eine Eigenschaft der Datenbankdatei selbst, nicht der Verbindung, bleibt über Neustarts hinweg bestehen und wird von jeder künftigen Verbindung (auch von anderen Tools) übernommen, bis es explizit zurückgesetzt wird. Dieses Attribut später wieder auf False zu setzen, macht eine bereits umgestellte Datei nicht rückgängig."
            en: "SQLite3 only: Set to True to switch the database file to WAL journal mode - lets a write proceed alongside reads instead of blocking them (relevant since this plugin keeps two independent connections open to the same file). WARNING: this is effectively a one-way decision - WAL is a property of the database file itself, not the connection, persists across restarts, and is picked up by any future connection (including other tools) until explicitly switched back. Setting this attribute back to False later does not revert an already-converted file."
    prefix:
        type: str
        default: ''
        description:
            de: 'Enthält ein Prefix welches vor die Datenbanktabellen des Plugins geschrieben wird'
            en: "if you want to log into an existing database with other tables you can specify a prefix for the plugins' tables"
    cycle:
        type: int
        default: 60
        description:
            de: 'Dump Cycle bestimmt wie oft die Daten auf Platte geschrieben werden (in Sekunden).'
            en: 'Dump cycle defines how often the database is dumped to disc (in seconds).'
    removeold_cycle:
        type: int
        default: 91
        description:
            de: 'Remove old Cycle bestimmt wie oft die Prüfung auf veraltete Daten gestartet werden soll (in Sekunden).'
            en: 'Remove old cycle defines how often the database will be checked for old data (in seconds).'
    precision:
        type: int
        default: 2
        description:
            de: 'Genauigkeit der aus der Datenbank ausgelesenen Werte (Nachkommastellen).'
            en: 'Precision of values read from database (digits after comma).'

    time_precision:
        type: int
        default: 3
        valid_min: 0
        valid_max: 3
        description:
            de: 'Genauigkeit der aus der Datenbank ausgelesenen Zeitwerte (Nachkommastellen (für Sekunden)).'
            en: 'Precision of time values read from database (digits after comma (for seconds)).'

    count_logentries:
        type: bool
        default: False
        description:
            de: "Auf True setzen, um für das Web Interface die Anzahl der Logeinträge für jedes Item zu zählen und als extra Spalte anzuzeigen"
            en: "Set to True to count the number of log entries for each item for the web interface and show as extra column"

    max_delete_logentries:
        type: int
        default: 20000
        valid_min: 1000
        description:
            de: "Maximal auf einmal zu löschende Anzahl an Log Einträgen mit dem database_maxage Attribut, reduziert die Belastung der Datenbank bei alten Datenbeständen"
            en: "Maximum number of Logentries to delete at once with database_maxage attribute, reduces load on database with old datasets"

    max_reassign_logentries:
        type: int
        default: 20  # 000
        valid_min: 10  # 00
        description:
            de: "Maximal auf einmal neu zuzuweisende Anzahl an Log Einträgen, reduziert die Belastung der Datenbank bei großen Datenbeständen"
            en: "Maximum number of Logentries to reassign at once, reduces load on database with large datasets"

    default_maxage:
        type: int
        default: 0
        valid_min: 0
        description:
            de: "Falls dieser Parameter einen Wert größer 0 enthält: Standard maxage für Items, die kein maxage gesetzt haben"
            en: "If this parameter is > 0: maxage for Items that don't have a maxage set."

    default_maxage_action:
        type: str
        default: 'delete'
        valid_list_ci: ['delete', 'avg', 'sum', 'min', 'max', 'integrate', 'duty_cycle', 'on', 'countall', 'first', 'last']
        description:
            de: "Standard-Aktion für Items mit database_maxage, die kein eigenes database_maxage_action gesetzt haben. 'on' ist ein veralteter Alias für 'duty_cycle' und wird nur aus Kompatibilitätsgründen noch akzeptiert - unquoted 'on' wird von YAML als bool True interpretiert, daher 'duty_cycle' verwenden."
            en: "Default action for items with database_maxage that don't have their own database_maxage_action set. 'on' is a legacy alias for 'duty_cycle', accepted for backward compatibility only - unquoted 'on' gets parsed as boolean True by YAML, so use 'duty_cycle' instead."

    default_maxage_interval:
        type: str
        default: '24h'
        description:
            de: "Standard-Kompaktierungsintervall für Items, die kein eigenes database_maxage_interval gesetzt haben (nur relevant, falls database_maxage_action nicht 'delete' ist). Format wie bei 'cycle'/'autotimer' (z.B. '24h', '30m'), Tage-Suffix 'd' wird nicht unterstützt."
            en: "Default compaction interval for items that don't have their own database_maxage_interval set (only relevant if database_maxage_action is not 'delete'). Same format as 'cycle'/'autotimer' (e.g. '24h', '30m'), no 'd' (days) suffix supported."

    max_aggregate_intervals:
        type: int
        default: 30
        valid_min: 1
        description:
            de: "Maximale Anzahl an Kompaktierungs-Intervallen, die pro Durchlauf für ein Item verarbeitet werden, reduziert die Belastung der Datenbank bei alten Datenbeständen"
            en: "Maximum number of compaction intervals processed per item per run, reduces load on database with old datasets"

    invalid_check_cycle:
        type: int
        default: 60
        valid_min: 1
        description:
            de: "Bestimmt, wie oft (in Sekunden) auf Items mit database_invalid_after geprüft wird, die seither still geblieben sind. Nur relevant, falls mindestens ein Item database_invalid_after gesetzt hat."
            en: "How often (in seconds) items with database_invalid_after are checked for having gone silent since. Only relevant if at least one item has database_invalid_after set."

    invalid_check_grace_time:
        type: int
        default: 60
        valid_min: 0
        description:
            de: "Zusätzliche Karenzzeit (in Sekunden), die nach dem Start des Plugins zum database_invalid_after-Wert eines Items addiert wird, bevor es als ungültig markiert werden kann - deckt DB-Lesezeit, Startup-Verzögerungen und Scheduler-Streuung ab. Für die meisten Konfigurationen ist der Standardwert ausreichend."
            en: "Extra grace time (in seconds), added to an item's own database_invalid_after value, before it can be marked invalid following plugin startup - covers db read delays, startup lag, and scheduler scattering. The default is fine for most configurations."

    copy_database:
        type: bool
        default: False
        description:
            de: "Nur für SQLite3: Auf True setzen, um beim Start von SmartHomeNG eine Kopie der Datenbank Datei zu erzeugen"
            en: "For SQLite3 only: Set to True to make a copy of the database file on startup of SmartHomeNG"

    copy_database_name:
        type: str
        description:
            de: "Nur für SQLite3: Pfad/Name der Datenbank Kopie"
            en: "For SQLite3 only: Path/Name of the copy of the database file"

item_attributes:
    # Definition of item attributes defined by this plugin
    database:
        type: str
        valid_list_ci: ['', 'no', 'yes', 'init', 'true', 'false']
        duplicate_use: True
        description:
            de: "Wenn auf 'yes' oder 'true' gesetzt, werden die Werte des Items in die Datenbank geschrieben. Wenn auf 'init' gesetzt, wird zusätzlich beim Start von SmartHomeNG der Wert des Items aus der Datenbank gelesen."
            en: "This attribute enables the database logging when set (just use value 'yes' or 'true'). If value 'init' is used, an item will be initalized from the database after SmartHomeNG is restarted."

    database_maxage:
        type: num
        valid_min: 0
        description:
            de: "Maximales Alter (in Tagen) der Logeinträge, die für dieses Item gespeichert werden sollen. Falls nicht angegeben, werden die Werte unbegrenzt gespeichert."
            en: "Maximum age (in days) of the log entries to be saved for this item. If not specified, the values are saved indefinitely."

    database_maxage_action:
        type: str
        valid_list_ci: ['delete', 'avg', 'sum', 'min', 'max', 'integrate', 'duty_cycle', 'on', 'countall', 'first', 'last']
        description:
            de: "Aktion, die beim Erreichen von database_maxage auf ältere Logeinträge angewendet wird. 'delete' (Standardverhalten) löscht die Einträge ersatzlos; jeder andere Wert ersetzt sie durch einen Kompaktierungswert pro database_maxage_interval, statt sie zu löschen. 'duty_cycle' (nur bool-Items) berechnet den zeitgewichteten Ein-Anteil. 'on' ist ein veralteter Alias für 'duty_cycle', nur aus Kompatibilitätsgründen noch akzeptiert - unquoted 'on' wird von YAML als bool True interpretiert, daher 'duty_cycle' verwenden. Kein Standardwert hier gesetzt, damit default_maxage_action als Fallback greifen kann, wenn das Attribut nicht gesetzt ist."
            en: "Action applied to log entries older than database_maxage. 'delete' (today's behaviour) removes them outright; any other value replaces them with one compacted value per database_maxage_interval instead of deleting them. 'duty_cycle' (bool items only) computes the time-weighted on-fraction. 'on' is a legacy alias for 'duty_cycle', accepted for backward compatibility only - unquoted 'on' gets parsed as boolean True by YAML, so use 'duty_cycle' instead. No default set here on purpose, so the default_maxage_action plugin parameter can act as fallback when the attribute is unset."

    database_maxage_interval:
        type: str
        description:
            de: "Kompaktierungsintervall im Format von 'cycle'/'autotimer' (z.B. '24h', '30m'; kein 'd'-Suffix), nur relevant falls database_maxage_action nicht 'delete' ist. Kein Standardwert hier gesetzt, damit default_maxage_interval als Fallback greifen kann, wenn das Attribut nicht gesetzt ist."
            en: "Compaction interval, same format as 'cycle'/'autotimer' (e.g. '24h', '30m'; no 'd' suffix), only relevant if database_maxage_action is not 'delete'. No default set here on purpose, so the default_maxage_interval plugin parameter can act as fallback when the attribute is unset."

    database_acl:
        type: str
        valid_list_ci: ['rw', 'ro']
        default: 'rw'
        description:
            de: "Muss normalerweise nicht konfiguriert werden, dann wird der Standard 'rw' genutzt. Es kann nützlich sein Werte als 'ro' zu deklarieren, falls die Datenbank Daten aus anderen Quellen enthält, die durch SmartNomeNG nicht verändert werden sollen."
            en: "Usually does not need to be configured, in this case the standard 'rw' is used. It can be useful to declare values as 'ro' if the database contains data from other sources that SmartNomeNG should not change."

    database_write_on_shutdown:
        type: bool
        default: True
        description:
            de: "Muss normalerweise nicht konfiguriert werden, dann wird der Standard 'True' genutzt. Das bedeutet, dass das item vor dem Beenden von smarthomeNG nochmal in die DB geschrieben wird (auch wenn keine echte Aenderung statt fand). Wird das Attribut auf 'False' gesetzt, wird das Schreiben beim Beenden unterdrueckt."
            en: "Usually does not need to be configured, in this case the default 'True' is used. This means that the item value is written to the DB once again on smarthomeNG shutdown. (even though the item has not been updated). Setting the attribute to 'False', supresses the rewrite on shutdown."

    database_invalid_after:
        type: str
        description:
            de: "Opt-in, pro Item: Maximale Stillezeit (Format wie 'cycle'/'autotimer', z.B. '5m', '30s'), nach der ein Item ohne Update automatisch als ungültig markiert wird (siehe db_mark_invalid() - Lücke statt gerader Interpolationslinie in Plots). Kein Standardwert und kein Plugin-Fallback - jedes Item muss dies explizit selbst setzen, da die richtige Schwelle von der jeweiligen Datenquelle abhängt. Erfordert das item-Attribut 'enforce_updates', sonst wird eine unveränderte Wiederholung desselben Werts nicht als Lebenszeichen erkannt (nur die periodische Prüfung bleibt dann voll wirksam). Nicht mit database_acl: ro kombinierbar."
            en: "Opt-in, per item: maximum silence duration (same format as 'cycle'/'autotimer', e.g. '5m', '30s') after which an item with no update is automatically marked invalid (see db_mark_invalid() - a gap instead of a straight interpolated line in plots). No default value and no plugin-level fallback - every item must set this explicitly itself, since the right threshold depends on that specific data source. Requires the item attribute 'enforce_updates', otherwise an unchanged repeat of the same value isn't recognised as a sign of life (only the periodic check stays fully effective). Not compatible with database_acl: ro."


item_structs:
    # Companion items mirroring the parent's own value, each independently
    # compacted with a different database_maxage_action. Attach via
    # 'struct: database.min' / 'struct: database.max' on any item that
    # itself sets database_maxage/database_maxage_action. See
    # database_maxage_action documentation for why min/max need a separate
    # item rather than being an option list on the parent's own attribute.
    #
    # The struct key is bare 'min'/'max' (already namespaced by the plugin
    # name at the reference point, struct: database.min), but the child
    # item it creates is named 'db_min'/'db_max' - once instantiated, a
    # bare '<item>.min' would be indistinguishable from an unrelated
    # hand-authored item of the same name (e.g. a configured setpoint);
    # 'db_min'/'db_max' stays self-explanatory regardless of how it got
    # there.
    #
    # Deliberately NOT copying database_maxage/database_maxage_interval/
    # enforce_updates from the parent via '..:.' here: that only works if
    # the parent sets them explicitly, which defeats the point of having
    # default_maxage/default_maxage_action/default_maxage_interval plugin
    # parameters in the first place (an item can legitimately set only
    # database_maxage_action and rely on the plugin-level default for
    # everything else). Copying an attribute the parent doesn't actually
    # have produces an empty string, which crashed item creation outright
    # (database_maxage) or logged a parse warning (enforce_updates) instead
    # of silently doing nothing. Leaving these unset lets the companion
    # resolve database_maxage/database_maxage_interval through the exact
    # same plugin-level fallback the parent itself would use if it also
    # left them unset - correct in the common case. The one case this
    # doesn't cover: a parent with an explicit *non-default* database_maxage
    # or enforce_updates:true - the companion won't automatically mirror
    # that, and would need a local override alongside the struct reference.
    min:
        on_change: .db_min = value
        db_min:
            type: num
            database: true
            database_maxage_action: min

    max:
        on_change: .db_max = value
        db_max:
            type: num
            database: true
            database_maxage_action: max

logic_parameters: NONE
    # Definition of logic parameters defined by this plugin

plugin_functions:
    # Definition of function interface of the plugin
    id:
        type: int
        description:
            de: 'Liefert für das angegebene Item die Datenbank-ID zurück'
            en: 'Return the database ID for the given item'
        parameters:
            item:
                type: foo
                description:
                    de: "Das Item-Objekt"
                    en: "An item object"
            create:
                type: bool
                description:
                    de: "Falls True, wird das Item in der Datenbank angelegt, falls es dort noch nicht existiert"
                    en: "If True, the item is created within the database if it does not exist"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    db:
        type: foo
        description:
            de: 'Liefert das Lowlevel Datenbank-Objekt zurück'
            en: 'Returns the low-level database object'

    dump:
        type: void
        description:
            de: 'Erzeugt einen Datenbank-Dump für angegebene Kriterien'
            en: 'Creates a database dump for given criterias'
        parameters:
            dumpfile:
                type: str
                description:
                    de: "Dateiname in die der Dump geschrieben werden soll"
                    en: "Filename to write dump to"
            id:
                type: int
                description:
                    de: "Dump auf angegebene Item-ID eingeschränken (optional)"
                    en: "Restrict dump to given item ID (optional)"
            time:
                type: int
                description:
                    de: "Dump auf die angegebene Zeit eingeschränken (optional)"
                    en: "Restrict dump to given time (optional)"
            time_start:
                type: int
                description:
                    de: "Dump auf die Startzeit eingeschränken (optional)"
                    en: "Restrict dump to given start time (optional)"
            time_end:
                type: int
                description:
                    de: "Dump auf die Endzeit eingeschränken (optional)"
                    en: "Restrict dump to given end time (optional)"
            changed:
                type: int
                description:
                    de: "Dump auf die angegebene Zeit einer Änderung eingeschränken (optional)"
                    en: "Restrict dump to given time of change (optional)"
            changed_start:
                type: int
                description:
                    de: "Dump auf die angegebene Startzeit von Änderungen eingeschränken (optional)"
                    en: "Restrict dump to given start time of changes (optional)"
            changed_end:
                type: int
                description:
                    de: "Dump auf die angegebene Endzeit von Änderungen eingeschränken (optional)"
                    en: "Restrict dump to given end time of changes (optional)"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    insertItem:
        type: int
        description:
            de: 'Item-Datenbankeintrag für angegebene Datenbank-ID anlegen'
            en: 'Create database item record for given database ID'
        parameters:
            name:
                type: str
                description:
                    de: "Name des Items für das ein Eintrag angelegt werden soll"
                    en: "name of item to create a record for"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    updateItem:
        type: void
        description:
            de: 'Item-Datenbankeintrag für angegebene Datenbank-ID aktualisieren'
            en: 'Update database item record for given database ID'
        parameters:
            id:
                type: int
                description:
                    de: "Datenbank-ID des Items für das der Eintrag aktualisiert werden soll"
                    en: "Database ID of item to update the record for"
            time:
                type: int
                description:
                    de: "Zeitpunkt an dem der Wert aktiv war"
                    en: "Time for the given value"
            duration:
                type: int
                description:
                    de: "Zeitintervall für den der Wert aktiv war"
                    en: "Time duration for the given value"
            val:
                type: str
                description:
                    de: "Der Wert der in die Datenbank geschrieben werden soll"
                    en: "The value to write to the database"
            it:
                type: str
                description:
                    de: "Der Item-Typ des Wertes ('str', 'num', 'bool')"
                    en: "The item type of the value ('str', 'num', 'bool')"
            changed:
                type: int
                description:
                    de: "Zeitstempel der Änderung"
                    en: "Time of change"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    readItem:
        type: foo
        description:
            de: "Item-Datenbankeintrag für angegebene Datenbank-ID auslesen

                 Diese Methode liest die Item-Daten einschließlich aller Felder. Wenn der ID Parameter ein String
                 ist, wird angenommen, dass das Element über den Item Namen ausgewählt werden soll
                 und nicht die Item-ID.
                "
            en: "Read database item record for given database ID

                 This method will read the item data including all fields. If the id
                 parameter is a string it is assumed that the item should be selected by
                 the items name and not by the items ID.
                "
        parameters:
            id:
                type: int/str
                description:
                    de: "Datenbank-ID des Items für das der Eintrag ausgelesen werden soll"
                    en: "Database ID of item to read the record for"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    readItems:
        type: foo
        description:
            de: 'Alle Item-Datenbankeinträge incl. aller Felder auslesen'
            en: 'This method will read all items data including all fields.'
        parameters:
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    deleteItem:
        type: void
        description:
            de: 'Löschen eines Item-Datenbankeintrags und von dessen Log Daten'
            en: 'This method will delete the item and its log data'
        parameters:
            id:
                type: int
                description:
                    de: "Datenbank-ID des Items für das der Eintrag gelöscht werden soll"
                    en: "Database ID of item to delete the record for"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    insertLog:
        type: void
        description:
            de: 'Log-Datenbankeintrag für angegebene Datenbank-ID anlegen'
            en: 'Create database log record for given database ID'
        parameters:
            id:
                type: int
                description:
                    de: "Datenbank-ID des Items für das ein Eintrag angelegt werden soll"
                    en: "Database ID of item to create a record for"
            time:
                type: int
                description:
                    de: "Zeitpunkt an dem der Wert aktiv war"
                    en: "Time for the given value"
            duration:
                type: int
                description:
                    de: "Zeitintervall für den der Wert aktiv war"
                    en: "Time duration for the given value"
            val:
                type: str
                description:
                    de: "Der Wert der in die Datenbank geschrieben werden soll"
                    en: "The value to write to the database"
            it:
                type: str
                description:
                    de: "Der Item-Typ des Wertes ('str', 'num', 'bool')"
                    en: "The item type of the value ('str', 'num', 'bool')"
            changed:
                type: int
                description:
                    de: "Zeitstempel der Änderung"
                    en: "Time of change"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"
            quality:
                type: int
                description:
                    de: "Daten-Qualitäts-Flag (Standard: QUALITY_VALID, QUALITY_NO_DATA für eine Lücken-Zeile)"
                    en: "Data-quality flag (QUALITY_VALID by default, QUALITY_NO_DATA for a gap row)"

    updateLog:
        type: void
        description:
            de: 'Log-Datenbankeintrag für angegebene Datenbank-ID aktualisieren'
            en: 'Update database log record for given database ID'
        parameters:
            id:
                type: int
                description:
                    de: "Datenbank-ID des Items für das der Eintrag aktualisiert werden soll"
                    en: "Database ID of item to update the record for"
            time:
                type: int
                description:
                    de: "Zeitpunkt an dem der Wert aktiv war"
                    en: "Time for the given value"
            duration:
                type: int
                description:
                    de: "Zeitintervall für den der Wert aktiv war"
                    en: "Time duration for the given value"
            val:
                type: str
                description:
                    de: "Der Wert der in die Datenbank geschrieben werden soll"
                    en: "The value to write to the database"
            it:
                type: str
                description:
                    de: "Der Item-Typ des Wertes ('str', 'num', 'bool')"
                    en: "The item type of the value ('str', 'num', 'bool')"
            changed:
                type: int
                description:
                    de: "Zeitstempel der Änderung"
                    en: "Time of change"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"
            quality:
                type: int
                description:
                    de: "Daten-Qualitäts-Flag (Standard: QUALITY_VALID, QUALITY_NO_DATA für eine Lücken-Zeile)"
                    en: "Data-quality flag (QUALITY_VALID by default, QUALITY_NO_DATA for a gap row)"

    readLog:
        type: foo
        description:
            de: 'Log-Datenbankeintrag für angegebene Datenbank-ID auslesen'
            en: 'Read database log record for given database ID'
        parameters:
            id:
                type: int
                description:
                    de: "Datenbank-ID des Logs für das der Eintrag gelesen werden soll"
                    en: "Database ID of log to read the record for"
            time:
                type: int
                description:
                    de: "Zeitpunkt an dem der Wert aktiv war"
                    en: "Time for the given value"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    readLogs:
        type: foo
        description:
            de: 'Log-Datenbankeinträge für angegebene Datenbank-ID auslesen'
            en: 'Read database log records for given database ID'
        parameters:
            id:
                type: int
                description:
                    de: "Datenbank-ID des Logs für das der Eintrag aktualisiert werden soll"
                    en: "Database ID of log to read the records for"
            time:
                type: int
                description:
                    de: "Auslesen auf angegebene Zeit eingeschränken (optional)"
                    en: "Restrict reading of records to given time (optional)"
            time_start:
                type: int
                description:
                    de: "Auslesen auf angegebene Startzeit eingeschränken eingeschränken (optional)"
                    en: "Restrict reading of records to given start time (optional)"
            time_end:
                type: int
                description:
                    de: "Auslesen auf angegebene Endzeit eingeschränken eingeschränken (optional)"
                    en: "Restrict reading of records to given end time (optional)"
            changed:
                type: int
                description:
                    de: "Auslesen auf angegebene Änderungszeit eingeschränken (optional)"
                    en: "Restrict reading of records to given change time (optional)"
            changed_start:
                type: int
                description:
                    de: "Auslesen auf angegebene Start der Änderungszeit eingeschränken eingeschränken (optional)"
                    en: "Restrict reading of records to given start time of changes (optional)"
            changed_end:
                type: int
                description:
                    de: "Auslesen auf angegebene Ende der Änderungszeit eingeschränken eingeschränken (optional)"
                    en: "Restrict reading of records to given end time of changes (optional)"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    deleteLog:
        type: void
        description:
            de: 'Log-Datenbankeinträge für angegebene Datenbank-ID löschen'
            en: 'Delete database log records for given database ID'
        parameters:
            id:
                type: int
                description:
                    de: "Datenbank-ID für die Einträge gelöscht werden sollen"
                    en: "Database ID of item to delete the records for"
            time:
                type: int
                description:
                    de: "Löschen auf angegebene Zeit eingeschränken (optional)"
                    en: "Restrict deletion of records to given time (optional)"
            time_start:
                type: int
                description:
                    de: "Löschen auf angegebene Startzeit eingeschränken eingeschränken (optional)"
                    en: "Restrict deletion of records to given start time (optional)"
            time_end:
                type: int
                description:
                    de: "Löschen auf angegebene Endzeit eingeschränken eingeschränken (optional)"
                    en: "Restrict deletion of records to given end time (optional)"
            changed:
                type: int
                description:
                    de: "Löschen auf angegebene Änderungszeit eingeschränken (optional)"
                    en: "Restrict deletion of records to given change time (optional)"
            changed_start:
                type: int
                description:
                    de: "Löschen auf angegebene Start der Änderungszeit eingeschränken eingeschränken (optional)"
                    en: "Restrict deletion of records to given start time of changes (optional)"
            changed_end:
                type: int
                description:
                    de: "Löschen auf angegebene Ende der Änderungszeit eingeschränken eingeschränken (optional)"
                    en: "Restrict deletion of records to given end time of changes (optional)"
            cur:
                type: foo
                description:
                    de: "Ein Datenbankcursor Objekt, falls vorhanden (optional)"
                    en: "A database cursor object if available (optional)"

    cleanup:
        type: void
        description:
            de: "Datenbank aufräumen (löscht ungenutzte Item/Log Einträge aus der Datenbank)

                 Diese Methode entfernt alle Elemente und Logs aus der Datenbank der Elemente, welche derzeit nicht
                 für die Protokollierung in der Datenbank konfiguriert sind. Vorsicht vor dieser Verwendung
                 in einem Setup mit mehreren Plugin Instanzen, da eine Instanz die Items der anderen Instanz nicht kennt!
                "
            en: "Cleanup database (deletes unused item/log records in the database)

                 This method will remove all items and logs from database of items which
                 are currenlty not configured to be logged to database. Beware of this using
                 in a multi-instance setup, since one instance does not know the item of
                 the other instance!
            "
