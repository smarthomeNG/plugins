main:

    num:
        type: num
        database: init

    str:
        type: str
        database: init

    bool:
        type: bool
        database: init

    nodb:
        type: num

    maxage:
        type: num
        database: init
        database_maxage: 30
