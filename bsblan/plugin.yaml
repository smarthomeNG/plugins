# Metadata for the plugin
plugin:
    # Global plugin attributes
    type: interface                   # plugin type (gateway, interface, protocol, system, web)
    description:
        de: 'Plugin zur Datenabfrage des BSB-LAN-Adapters'
        en: 'Plugin to request data from your BSB-LAN-Adapter'
    maintainer: Thomas Hengsberg (thengsty)
    tester:
    state: develop                  # change to ready when done with development
#    keywords: iot xyz
    documentation:
    support:

    version: 1.0.2                  # Plugin version
    sh_minversion: '1.8'              # minimum shNG version to use this plugin
#    sh_maxversion:                 # maximum shNG version to use this plugin (leave empty if latest)
    py_minversion: '3.7'              # minimum Python version to use this plugin
#    py_maxversion:                 # maximum Python version to use this plugin (leave empty if latest)
    multi_instance: False           # plugin supports multi instance
    restartable: unknown
    classname: Bsblan               # class containing the plugin

parameters:
    bsblan_ip:
        type: str
        default: ''
        description:
            de: 'IP Adresse des BSB-Lan Adapters'
            en: 'IP address of the BSB-LAN Adapter'

item_attributes: NONE
    # Definition of item attributes defined by this plugin (enter 'item_attributes: NONE', if section should be empty)

item_structs: NONE
    # Definition of item-structure templates for this plugin (enter 'item_structs: NONE', if section should be empty)
    #item_structs: NONE

plugin_functions: NONE
    # Definition of plugin functions defined by this plugin (enter 'plugin_functions: NONE', if section should be empty)
    #plugin_functions: NONE

logic_parameters: NONE
    # Definition of logic parameters defined by this plugin (enter 'logic_parameters: NONE', if section should be empty)
    #logic_parameters: NONE

